{
  "name": "swiper-from-library",
  "version": "1.0.0",
  "description": "",
  "source": "src/index.html",
  "scripts": {
    "clear": "rm -rf dist",
    "start": "npm run clear && parcel",
    "build": "npm run clear && parcel build",
    "predeploy": "rm -rf dist && parcel build ./*.html --public-url https://github.com/TrueForFun/using-javascript-in-websites",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "popmotion": "^8.5.0"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "parcel": "^2.8.3"
  }
}
